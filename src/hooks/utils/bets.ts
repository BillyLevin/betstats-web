export enum BET_STATES {
    idle = 'idle',
    loading = 'loading',
    success = 'success',
    failure = 'failure',
}

export enum BET_ACTIONS {
    FORM_SUBMIT = 'FORM_SUBMIT',
    API_SUCCESS = 'API_SUCCESS',
    API_FAILURE = 'API_FAILURE',
}
